
/// Object representing a snack when constructing an order on the GUI
class snack_order{
  String name;
  double price;
  int table_index;
  snack_order({required this.name, required this.price, required this.table_index});
}